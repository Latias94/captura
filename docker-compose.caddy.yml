version: "3.9"

services:
  caddy:
    image: caddy:2-alpine
    container_name: captura-caddy
    ports:
      - "80:80"
    command: ["caddy", "reverse-proxy", "--from", ":80", "--to", "api:8080"]
    depends_on:
      api:
        condition: service_started
    restart: unless-stopped

